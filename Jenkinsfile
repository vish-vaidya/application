pipeline {
    
    agent any
        
        stages {
            
            stage ('stage-1') {
            
            steps {
                
                sh "cp -r index.html /var/www/html/index.html"
            }
        }
    }
}
